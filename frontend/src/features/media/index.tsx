import Heading from '@atlaskit/heading';

// Phase 0 stub — full upload flow (presign → S3 PUT → complete-upload) in Phase 0 walking skeleton
export function MediaUpload() {
  return (
    <section>
      <Heading size="medium">Media Upload</Heading>
      <p style={{ color: 'var(--ds-text-subtle)', marginTop: '0.5rem' }}>
        Upload coming in Phase 0 (docs/05 §2.2).
      </p>
    </section>
  );
}
