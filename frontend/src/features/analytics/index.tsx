import Heading from '@atlaskit/heading';

// Phase 0 stub — YouTube/X OAuth + publish + metrics in Phase 1 analytics module
export function AnalyticsDashboard() {
  return (
    <section>
      <Heading size="medium">Analytics</Heading>
      <p style={{ color: 'var(--ds-text-subtle)', marginTop: '0.5rem' }}>
        Analytics coming in Phase 1 (docs/05 §2.3).
      </p>
    </section>
  );
}
